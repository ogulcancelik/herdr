name: PR Gate (disabled on fork)

on:
  pull_request_target:
    types: []

jobs:
  check-contributor:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "PR gate bypassed on fork — all contributors are approved"'
